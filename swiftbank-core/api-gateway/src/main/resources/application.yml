spring:
  application:
    name: api-gateway

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICE_URL_DEFAULT_ZONE}

jwt:
  secret-key: ${JWT_SECRET_KEY}

server:
  port: ${GATEWAY_SERVER_PORT}
